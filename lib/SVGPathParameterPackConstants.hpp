#pragma once

namespace ngPlasmid
{
    namespace SVGPathParameterPacks
    {
        namespace plasmidTrack
        {
            const int centerX = 0;
            const int centerY = 1;
            const int radius = 2;
            const int width = 3;
        }

        namespace trackMarker
        {
            const int centerX = 0;
            const int centerY = 1;
            const int radiusInner = 2;
            const int angleStart = 3;
            const int angleEnd = 4;
            const int width = 5;
            const int arrowStartAngle = 6;
            const int arrowStartLength = 7;
            const int arrowStartWidth = 8;
            const int arrowEndAngle = 9;
            const int arrowEndLength = 10;
            const int arrowEndWidth = 11;
        }
    }
}