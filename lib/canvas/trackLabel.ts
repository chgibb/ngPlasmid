import {TrackLabel} from "./../directives";
import {parseStyle} from "./../parseStyle";
import {drawSVGarcOnCanvas} from "./utils";

export function trackLabelToCanvas(track : TrackLabel,ctx : CanvasRenderingContext2D) : void
{
    track;
    ctx;
}